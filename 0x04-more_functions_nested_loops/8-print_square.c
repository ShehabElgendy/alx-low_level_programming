#include "main.h"

/**
 * print_square - Print square lines.
 * @n: Number of lines
 *
 * Return: There is no return value
 */
void print_square(int size)
{
    int i;
    int n;
    
    if (n > 0)
    {
        for (i = 0; i <= n; i++);
        _putchar("#");
    }
}